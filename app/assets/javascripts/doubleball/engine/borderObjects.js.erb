GameCreator.borderObjects = {
    borderL: {
        objectName: "borderL", 
        parent: {id: -1}, 
        id: -1, 
        isCollidable: true,
        attributes: {
            x: -500, 
            y: -500, 
            height: GameCreator.props.height + 1000, 
            width: 500,
            speedX: 0,
            speedY: 0,
            instanceId: 'borderL',
            image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderLeft.png' %>"; return img; }()),
        },
        image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderLeft.png' %>"; return img; }()),
        collidesWith: function(object) {
            return object.attributes.x <= 0;
        }
    },
    borderR: {
        objectName: "borderR", 
        parent: {id: -2}, 
        id: -2, 
        isCollidable: true,
        attributes: {
            x: GameCreator.props.width, 
            y: -500,
            speedX: 0,
            speedY: 0,
            height: GameCreator.props.height + 1000, 
            width: 500, 
            instanceId: 'borderR',
            image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderRight.png' %>"; return img; }()),
        },
        image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderRight.png' %>"; return img; }()),
        collidesWith: function(object) {
            return object.attributes.x + object.attributes.width >= GameCreator.props.width;
        }
    },
    borderT: {
        objectName: "borderT", 
        parent: {id: -3}, 
        id: -3, 
        isCollidable: true,
        attributes: {
            x: -500, 
            y: -500,
            speedX: 0,
            speedY: 0,
            height: 500, 
            width: GameCreator.props.width + 1000,
            instanceId: 'borderT',
            image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderTop.png' %>"; return img; }()),
        },
        image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderTop.png' %>"; return img; }()),
        collidesWith: function(object) {
            return object.attributes.y <= 0;
        }
    },
    borderB: {
        objectName: "borderB", 
        parent: {id: -4}, 
        id: -4, 
        isCollidable: true,
        attributes: {
            x: -500, 
            y: GameCreator.props.height, 
            height: 500,
            speedX: 0,
            speedY: 0, 
            width: GameCreator.props.width + 1000,
            instanceId: 'borderB',
            image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderBottom.png' %>"; return img; }()),
        },
        image: (function() {var img = (new Image()); $(img).css("width", "65"); img.src = "<%= asset_path 'game/borderBottom.png' %>"; return img; }()),
        collidesWith: function(object) {
            return object.attributes.y + object.attributes.height >= GameCreator.props.height;
        }
    },
}